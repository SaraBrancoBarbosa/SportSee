import { createContext } from "react"

/***** 
Create a React context. Used to share the "mode" state throughout the app
Initialisation: "null"
*****/

const Context = createContext(null)
export default Context